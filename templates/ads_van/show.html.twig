{% extends 'base.html.twig' %}

{% block title %}AdsVan{% endblock %}

{% block body %}
    <h1>AdsVan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ads_van.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ads_van.description }}</td>
                <td>{{ ads_van.floor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vanlifer_ads_van_index', {'slug': app.user.slug}) }}">back to list</a>

    <a href="{{ path('vanlifer_ads_van_edit', {'slug': app.user.slug, 'id': ads_van.id}) }}">edit</a>

    {{ include('ads_van/_delete_form.html.twig') }}
{% endblock %}
