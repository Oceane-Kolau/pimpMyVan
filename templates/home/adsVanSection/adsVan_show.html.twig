{% extends 'base.html.twig' %}

{% block title %}{{adsVan.title}}{% endblock %}

{% block body %}
{% include "navbar.html.twig" %}
<div class="wrapper">
    {% include "asidenav.html.twig" %}
    <div id="page-content-wrapper">
        <div class="container-fluid">
            <div class="row mx-auto justify-content-center align-items-center">
                
            </div>
            <div>
                <h1 class="mt-3 text-center">{{ adsVan.user }}</h1>
                <h1 class="mt-3 text-center">{{ adsVan.title }}</h1>
                <div class="row align-items-center justify-content-center mt-3">
                    <div class="col-auto">
                        
                    </div>
                    
                </div>
                <div class="col-xl-8 col-lg-8 col-md-7 col-sm-12 mr-5 mx-auto">
                    <div class="row pl-3 mt-4">
                        <div class="col-auto">
                            <p class="setup-type">{{ adsVan.specificSetup }}</p>
                        </div>
                        
                        <div class="col-auto">
                            <p class="setup-type">{{ adsVan.generalSetup }}</p>
                        </div>
                        <div class="row pl-3">
                            {% for specialtiesVan in adsVan.specialtiesVan %}
                            <div class="col-auto">
                                <p><i class="fas fa-plus plus-color"></i> {{ specialtiesVan.type }}</p>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="col-12">
                        <p class="p-2"> {{ adsVan.description }}</p>
                    </div>
                </div>
                {% if app.user is null %}
                <div class="col-xl-12 col-lg-12 col-md-12 col-xs-12">
                    <div class="text-center card-border p-3">
                        <p>Vous souhaitez demander un devis estimatif à cet adsVan ? <br>
                            Inscrivez-vous ou connectez-vous pour pouvoir le contacter !</p>
                        <div class="row justify-content-center">
                            <div class="col-auto">
                                <a href="{{path('app_login')}}">
                                    <button type="button" class="btn btn-turquoise">Connexion</button>
                                </a>
                            </div>
                            <div class="col-auto">
                                <a href="{{path('app_register_vanlifer')}}">
                                    <button type="button" class="btn btn-grey">S'inscrire</button>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                {% else %}
                <div class="col-xl-12 col-lg-12 col-md-12 col-xs-12">
                    <div class="text-center card-border p-3">
                        <div class="row justify-content-center">
                            <div class="col-auto">
                                
                            </div>
                        </div>
                    </div>
                </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}