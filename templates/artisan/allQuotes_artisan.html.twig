{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
<div class="wrapper">
    {% include "asidenav.html.twig" %}
    <div id="page-content-wrapper">
        <div class="container-fluid color-account">
            <div class="row mb-4">
                <div class="col-2">
                    <a class="navbar-brand ml-5 pl-5" href="/">
                        {% include 'logoSvg.html.twig' %}
                    </a>
                </div>
                {% include 'artisan/presentation_artisan.html.twig' with {artisan: artisan} only %}
            </div>
            <div class="row justify-content-around align-items-center">
                <div class="col-xl-2 col-lg-2 col-md-2 col-sm-12 text-center ml-4">
                    <a href="{{ path('artisan_profile', {'slug': artisan.slug, 'id': artisan.id}) }}">
                        <button type="button" class="btn btn-lg btn-grey m-3">Profil</button>
                    </a>
                    <a href="{{ path('artisan_messagerie', {'slug': artisan.slug, 'id': artisan.id}) }}">
                        <button type="button" class="btn btn-lg btn-grey m-3">Messages</button>
                    </a>
                    <a href="{{ path('artisan_profile', {'slug': artisan.slug, 'id': artisan.id}) }}">
                        <button type="button" class="btn btn-lg btn-grey m-3">Devis</button>
                    </a>
                </div>
                <div class="col-xl-8 col-lg-8 col-md-7 col-sm-12 mr-5 bg-lightgrey mx-auto p-5">
                    {% for quote in quotes|reverse %}
                    <div class="row justify-content-around align-items-center">
                        <div class="col-auto">
                            <p>{{ quote.firstname }} {{ quote.lastname }}</p>
                        </div>
                        <div class="col-auto d-none d-sm-block">
                            <p>{{ quote.projectDate|date('d M Y') }}</p>
                        </div>
        
                        <div class="col-auto">
                            <p>{{ quote.createdAt|date('d M Y')}}</p>
                        </div>
                            <a href="{{ path('artisan_quote_show', {'slug': artisan.slug, 'id': quote.id}) }}"><button class="btn btn-turquoise"><i class="far fa-eye"></i></button></a></td>
                    </div>
                    <hr>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}