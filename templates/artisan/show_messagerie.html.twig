{% extends 'profile_template/profile_template.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block content %}

<div class="row">
    <div class="col-12">
        <h4>{{ contact.firstname }} {{ contact.lastname }}</h4>
        <p>{{ contact.email }}</p>
        <p>{{ contact.subject }}</p>
        <p> {{ contact.message }}</p>
        <p>{{ contact.createdAt|date('d M Y')}}</p>
    </div>
</div>
<div class="col-11 text-right pr-5 pt-5">
    <a class="hover-link" href="{{ path('artisan_messagerie', {'slug': artisan.slug})}}">
        <p><i class="fas fa-arrow-left"></i> Retour à la liste des messages</p>
    </a>
</div>

{% endblock %}
