{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('artisan/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('artisan_profile') }}">back to list</a>

{% endblock %}
