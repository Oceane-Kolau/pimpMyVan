{% extends 'profile_template/profile_template.html.twig' %}

{% block title %}Mes messages{% endblock %}

{% block content %}

{% for contact in contacts|reverse %}
<div class="row justify-content-around align-items-center">
    <div class="col-auto">
        <p>{{ contact.firstname }} {{ contact.lastname }}</p>
    </div>
    <div class="col-auto d-none d-sm-block">
        <p>{{ contact.subject }}</p>
    </div>

    <div class="col-auto">
        <p>{{ contact.createdAt|date('d M Y')}}</p>
    </div>
    <a href="{{ path('artisan_messagerie_show', {'slug': artisan.slug, 'id': contact.id}) }}"><button
            class="btn btn-turquoise"><i class="far fa-eye"></i></button></a></td>
</div>
<hr>
{% endfor %}

{% endblock %}